# Neon PostgreSQL connection string — Agent DB asks for this before Phase 1 migration
DATABASE_URL="postgresql://user:password@ep-xxx.neon.tech/campusos?sslmode=require"

# Google Gemini API key — Agent AI asks for this before Phase 4 live testing
GEMINI_API_KEY="your-key-here"

PORT=4000
FRONTEND_ORIGIN="http://localhost:5173"

# NOT a secret — safe to commit. Resolves "my next class" / "register me".
# 20-40532 / Sakibul Hassan recurs as a registrant in the seed events data.
DEFAULT_STUDENT_ID="20-40532"
DEFAULT_STUDENT_NAME="Sakibul Hassan"
